apiVersion: v1
kind: Service
metadata:
  name: {{ include "feedback-backend.fullname" . }}
  labels:
    {{- include "feedback-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "feedback-backend.selectorLabels" . | nindent 4 }}
