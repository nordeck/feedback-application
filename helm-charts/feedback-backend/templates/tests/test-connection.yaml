apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "feedback-backend.fullname" . }}-test-connection"
  labels:
    {{- include "feedback-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "feedback-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
